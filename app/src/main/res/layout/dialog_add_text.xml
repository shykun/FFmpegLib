<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="#B3000000"
                android:orientation="vertical">

    <TextView
            android:id="@+id/addTextDoneButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_alignParentRight="true"
            android:layout_alignParentTop="true"
            android:layout_margin="20dp"
            android:padding="10dp"
            android:text="Done"
            android:textAllCaps="false"
            android:textSize="20sp"/>

    <EditText
            android:id="@+id/addTextInput"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_above="@+id/addTextColorPickerContainer"
            android:layout_below="@+id/addTextDoneButton"
            android:background="@null"
            android:gravity="center"
            android:inputType="textMultiLine"
            android:textSize="40sp"/>

    <RelativeLayout
            android:id="@+id/addTextColorPickerContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true">

        <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/addTextColorPickerRecyclerView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                tools:background="@android:color/black"/>

    </RelativeLayout>

</RelativeLayout>